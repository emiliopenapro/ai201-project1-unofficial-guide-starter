# Versions bumped for Python 3.14 wheel availability (see DEC-006).
sentence-transformers==5.5.1
chromadb==1.5.9
groq==1.4.0
python-dotenv==1.2.2

# Query interface (Milestone 5):
gradio==6.16.0

# Document ingestion — documents/ folder contains a .pdf, so this is required:
pdfplumber==0.11.9
